# hackerrank-counting-sort
From the Hacker Rank challenge "Counting Sort". The function returns a frequency array with 100 elements that counts and returns the number of times each value appears as an array of integers. 
